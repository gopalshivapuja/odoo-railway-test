<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Template overrides are handled via JS (header_social.js) because
         theme_ prefixed modules store templates as theme.ir.ui.view
         which requires manual theme application via the website builder.
         JS assets load directly through the manifest. -->
</odoo>
